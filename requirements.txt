# FinSage AI — Dependencies
# Python 3.11+

# === Agent Framework ===
langgraph>=0.2.0
langchain-google-genai>=2.0.0
langchain-core>=0.3.0
google-generativeai>=0.8.0

# === PDF Parsing ===
pdfplumber>=0.11.0
# camelot-py[cv] — optional, pulls in opencv (~500MB). Parser works without it.
# ghostscript — only needed if using camelot

# === Data Processing ===
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.12.0

# === Financial Data ===
mftool>=0.7.0

# === Vector DB / RAG (heavy — install separately when needed) ===
# chromadb>=0.5.0
# sentence-transformers>=3.0.0  # pulls in torch (~2GB)

# === Frontend ===
streamlit>=1.38.0
plotly>=5.22.0

# === PDF Report Generation ===
fpdf2>=2.8.0
reportlab>=4.0.0

# === Data Validation ===
pydantic>=2.6.0

# === Environment ===
python-dotenv>=1.0.0

# === Testing ===
pytest>=8.0.0
pytest-cov>=5.0.0
